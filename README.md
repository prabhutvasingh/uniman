# uniman
A universal package manager for every distro, it acts like a Windows .exe installer but for beginners on Linux. The unfortunate mindset of Windows users of not touching Microsoft Store moves on when they use Linux and they generally avoid touching built in stores, this simple Python code solves the hassle of similar .exe based installer.
